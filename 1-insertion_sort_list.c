#include <stdio.h>
#include "sort.h"
/**
 * insertion_sort_list- a function that sorts
 * a doubly linked list using insertion
 * @listint_t: the doubly linked list
 * @list- pointer to the node
 * Return: sorted doubly linked list
 */
